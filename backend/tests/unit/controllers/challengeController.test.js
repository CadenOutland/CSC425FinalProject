describe("ChallengeController (Mock Mode)", () => {
  test("get challenges mock", () => {
    expect(true).toBe(true);
  });

  test("create challenge mock", () => {
    expect(true).toBe(true);
  });
});
